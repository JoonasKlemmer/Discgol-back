@model App.Domain.DiscsInWishlist

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DiscsInWishlist</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="DiscFromPageId" class="control-label"></label>
                <select asp-for="DiscFromPageId" class="form-control" asp-items="ViewBag.DiscFromPageId"></select>
                <span asp-validation-for="DiscFromPageId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WishlistId" class="control-label"></label>
                <select asp-for="WishlistId" class="form-control" asp-items="ViewBag.WishlistId"></select>
                <span asp-validation-for="WishlistId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
